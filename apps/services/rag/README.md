# RAG Service (Legacy Microservice Stub)

> **Architectural Notice (ADR-0016)**: This directory contains an unintegrated legacy microservice stub. The active production RAG and knowledge retrieval system is unified within `apps/api/agi_server/okf` using ChromaDB as a disposable derived index with immutable OKF bundles as the source of truth and automatic lexical fallback.

